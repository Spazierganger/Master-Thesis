system:
  name: atomic
  training:
    collection_type: dynamic
    n_configs: 1
    config:
      symbol: Ne

  validation:
    collection_type: static
    n_configs: 1
    total_samples: 1000000
    config:
      symbol: Ne